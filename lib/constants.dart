import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xff191b31);
  static const card = Color(0xff323244);
  static const cardSelected = Color(0x73000000);
  static const accent = Color(0xffe83d66);
  static const label = Colors.grey;
  static const text = Colors.white;
}

class AppLimits {
  static const double minHeight = 100;
  static const double maxHeight = 220;
  static const int minWeight = 1;
  static const int maxWeight = 300;
  static const int minAge = 1;
  static const int maxAge = 120;
}
